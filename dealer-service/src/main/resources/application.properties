spring.application.name=dealer-service
server.port=8082

spring.datasource.url=jdbc:mysql://localhost:3306/dealerdb?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

eureka.client.service-url.defaultZone=http://localhost:8761/eureka
# Logging configuration for DealerService
logging.level.root=INFO
logging.level.com.cropdeal.dealer_service=DEBUG
logging.file.name=logs/dealer-service.log


# Spring Cloud Config client - to fetch config from Config Server
spring.cloud.config.uri=http://localhost:8888

# Logging level (optional)
logging.level.org.springframework=INFO